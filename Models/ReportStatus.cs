namespace VehicleWatch.Models
{
    // För att kunna hantera och redigera enkelt, används enum här.
    public enum ReportStatus
    {
        InProgress,
        Reapired,
        Completed,
    }
}
